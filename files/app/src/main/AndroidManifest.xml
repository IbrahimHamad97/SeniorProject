<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmps312.courseregistration">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.WaitingListActivity"></activity>
        <activity android:name=".view.HoldsActivity" />
        <activity android:name=".view.CourseManagementActivity" />
        <activity android:name=".view.StudentProfileActivity" />
        <activity android:name=".view.ScheduleActivity" />
        <activity android:name=".view.CourseRegistrationActivity" />
        <activity android:name=".view.MainActivity"/>
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>